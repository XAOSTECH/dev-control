# Environment
.bak
.tmp
.vscode
.devcontainer

# Test dependencies (auto-downloaded by run_tests.sh)
tests/test_helper/
test-repo

# User-specific configuration files (examples are tracked, real configs are not)
config/container.yaml
config/global.config.yaml
.devcontainer.yaml
# Build output
dist/

# Personal devcontainer config (generated by containerise.sh)
# Use _example or _minimal variants as tracked references
.devcontainer/Dockerfile
.devcontainer/devcontainer.json
